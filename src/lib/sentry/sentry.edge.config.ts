/**
 * Configuração Edge Sentry (Next.js: `sentry.edge.config.ts` — middleware em Vercel Edge).
 *
 * **Vite + SPA:** não aplicável. Em Next.js, importe e chame `Sentry.init` para capturar erros
 * em middleware e rotas edge.
 */
export {};
